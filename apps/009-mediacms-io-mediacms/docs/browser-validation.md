# 浏览器验收记录

验收日期：2026-09-04

目标：本地生产构建 `http://127.0.0.1:4209/`

## 桌面端：1280 × 800

- 首屏同时呈现“完整媒体平台”“研究价值按需”“部署成本较重”三项核心判断。
- 文档宽度 `1265px`，页面 `scrollWidth` 与 `clientWidth` 均为 `1265px`，无页面级横向溢出。
- 证据 JSON 成功加载 12 条固定提交来源。
- 控制台没有 error 或 warning。
- 架构页签通过点击显示“异步处理”完整四步；Home 切回第一项，End 切到最后一项。
- 评估器验证：私有媒体库 + 完整门户 + LMS 得到“建议进入技术验证 / 收益 7”；仅直播/低延迟/DRM 得到“当前不建议采用 / 成本 4”；重置恢复空状态。

## 手机端：390 × 844

- 标题、三项结论、主操作和研究结论卡按单列展示，文字无截断。
- 浏览器内容区 `375px`，`documentElement` 与 `body` 的 `scrollWidth`、`clientWidth` 均为 `375px`，无页面级横向溢出。
- 架构图谱变为双列与跨列组合，不再依赖横向滚动；页签在单行内完整显示。
- 紧凑目录可打开，Escape 可关闭并把焦点还给 summary。
- 控制台没有 error 或 warning。

## 边界

本记录验证的是研究 Demo 的信息、布局、交互与构建产物，不代表 MediaCMS 本体完成了部署、转码压力或故障恢复测试。
