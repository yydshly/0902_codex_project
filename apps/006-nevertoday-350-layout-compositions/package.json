{
  "name": "layout-compositions-capability-atlas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "node check.mjs",
    "preview": "npm run build && node serve.mjs",
    "serve": "node serve.mjs",
    "verify": "npm run build && npm run check"
  },
  "engines": {
    "node": ">=22"
  }
}
