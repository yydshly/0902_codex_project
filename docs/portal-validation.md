# 研究门户验证记录

验证日期：2026-09-03（Asia/Shanghai）

## 运行环境

- 构建：Node.js `v22.15.0`、npm `10.9.2`
- 浏览器自动化：Playwright Chromium，Node.js `v24.19.0`
- 本地静态服务：Python `3.12.13`
- 验证地址：`http://127.0.0.1:4173/`
- 验证对象：由 `npm run build` 生成的 `dist/`

浏览器截图和临时验证脚本保存在被 Git 忽略的 `.codex-tmp/`，不作为产品文件提交。

## 自动检查

| 检查 | 结果 |
| --- | --- |
| `npm run verify` | 通过；清单、README 索引、Demo 构建入口和 Pages 聚合均成功 |
| `git diff --check` | 通过 |
| Node.js 语法检查 | `site/app.js` 与四个 `scripts/*.mjs` 均通过 |
| 空清单 | 通过；`0` 个项目时仍生成完整门户和可行动空状态 |

## 浏览器覆盖

| 表面 / 状态 | 证据 | 结果 |
| --- | --- | --- |
| 1440 × 1000，浅色，空目录 | 全页截图、DOM 与页面错误监听 | 通过；层级清晰，无横向溢出，无页面异常 |
| 768 × 1024，深色，空目录 | 全页截图、DOM 与页面错误监听 | 通过；主题语义与布局保持一致 |
| 390 × 844，浅色，空目录 | 全页截图、DOM 与页面错误监听 | 通过；导航、按钮、统计和空状态均可用 |
| 已收录项目 | 注入两个乱序目录条目 | 通过；按 `001`、`002` 排序，状态筛选显示 `1 / 2`，Demo 子路径正确 |
| 目录加载失败 | 返回 HTTP 503 | 通过；错误说明与“重新读取”操作可见 |
| 键盘与焦点 | 390px 深色环境按一次 Tab | 通过；首个焦点为“跳到项目索引”，焦点轮廓可见 |
| reduced-motion | 模拟系统减少动态效果 | 通过；非必要动画和过渡缩短，不影响内容与操作 |

## 支持边界

- 门户是纯静态页面，不依赖后端、登录或外部字体。
- 主题跟随系统 `prefers-color-scheme`，当前不提供站内主题切换器。
- 项目详情继续在 GitHub 研究 README 中维护；Pages 负责摘要、筛选和 Demo 导航。
- 当前清单为空，因此生产门户展示正式空状态；有项目状态已通过浏览器夹具验证。
