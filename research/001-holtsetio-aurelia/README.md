# Aurelia：从程序化水母到 GPU 物理创意系统

> 一个紧凑的 Three.js WebGPU 案例，展示如何把解析主体动画、GPU 弹簧软体和程序化光色连接成无需外部美术资产的实时生命体。

[![Aurelia Field Atlas 首屏以大型中文标题说明从现实因果到交互作品的方法，右侧轨道图配合十项方向、十二个现实原型和十三层技术研究统计](assets/cover.png)](https://yydshly.github.io/0902_codex_project/demos/001-holtsetio-aurelia/atlas.html)

*核心演示图：研究 Demo 将 Aurelia 从单一水母案例扩展为“现实观察 → 物理语法 → GPU 状态 → 交互体验”的创意研究图谱。*

> 项目统一索引：[`notes/project-summary.md`](notes/project-summary.md) 汇总全部案例、已验证能力、研究结论、未完成能力与当前阶段决策；可运行网页入口为 [`summary.html`](../../apps/001-holtsetio-aurelia/summary.html)，[线上研究总账](https://yydshly.github.io/0902_codex_project/demos/001-holtsetio-aurelia/summary.html) 随 `main` 自动部署。

## 项目信息

| 字段 | 内容 |
| --- | --- |
| 研究编号 | `001` |
| 上游仓库 | [`holtsetio/aurelia`](https://github.com/holtsetio/aurelia/) |
| 研究基线 | [`724aba263459d1b11a49b72bfc7ce8baac840ca8`](https://github.com/holtsetio/aurelia/commit/724aba263459d1b11a49b72bfc7ce8baac840ca8) |
| 上游许可证 | [MIT License](https://github.com/holtsetio/aurelia/blob/724aba263459d1b11a49b72bfc7ce8baac840ca8/LICENSE) |
| 研究状态 | `validated` |
| 首次研究 | `2026-09-03` |
| 最近更新 | `2026-09-04` |
| 标签 | `three.js, webgpu, tsl, compute, procedural, soft-body, web-audio, audio-reactive` |

## 当前研究总账

本项目已经从单一源码拆解发展为一个由基准、工具、机制验证、业务纵切和旗舰探索共同组成的研究系统。为避免后续只看到页面数量而看不到证据边界，R39–R40 将当前状态收口为以下总账：

| 研究资产 | 当前规模 | 主要证据 | 当前判断 |
| --- | ---: | --- | --- |
| 质量基准 | 1 | 程序化水母的一因多果生命链 | 仍是唯一正式作品质量基准 |
| 可运行入口 | 16 | 基准、总账、实验室、图谱、业务页与 11 个研究 / 作品入口 | 足以覆盖研究，不再以新增页面作为进展指标 |
| 案例事实账 | 15 | 每项记录结构规模、输入、同源输出、验证与边界 | 可审计，不把机制通过写成作品成熟 |
| 创意方向 | 10 | LIVE / NEXT / PLANNED / RESEARCH 状态与下一实验 | 方向体系成立，尚未找到第二旗舰 |
| 现实映射 | 12 | 从雨伞、晾衣绳、地铁、墨迹到菌丝的计算抽象 | 已实现 5 类，其他保留为有边界候选 |
| 技术研究层 | 13 | 拓扑、GPU 状态、求解、表面、输入、声音、验证与产品边界 | 可抽取内核已识别，公共 runtime 尚未完成 |
| 业务纵切 | 1 | Weatherproof 的诊断 → 推荐 → 比较 → 咨询路径 | 业务链成立，数据仍为明确标注的合成演示 |
| 暂停探索 | 1 | Lumen Cuttlefish R31–R38 | 保留研究价值，冻结局部优化 |

统一网页采用“总览 → 案例 → 证据 → 能力 → 技术 → 基准 → 研究 → 演进 → 缺口 → 价值 → 决策”十一章结构。详细案例参数、能力矩阵、R0–R40 演进和来源索引均在[项目研究总账](https://yydshly.github.io/0902_codex_project/demos/001-holtsetio-aurelia/summary.html)中展开，交付契约与浏览器证据见 [`notes/project-summary-web.md`](notes/project-summary-web.md)。

## 为什么值得研究

Aurelia 不是通用框架，也不是已封装的 npm 组件。它的价值是用约 2,200 行 JavaScript 串起一条完整、可观察的 GPU 数据链：CPU 只创建拓扑和更新少量实例状态，GPU 负责弹簧计算、顶点重建、程序化材质和后处理。

这个项目尤其适合回答一个可迁移的问题：**如何让一个艺术可控的解析主动画驱动具有自然拖尾的次级软体，同时避免逐帧 CPU 回读？**

## 研究问题

- [x] 水母主体、钟口、口腕和触手分别如何生成与运动？
- [x] GPU 弹簧数据如何组织，解析钟体如何与软体节点连接？
- [x] 程序化材质、假体积光和选择性 Bloom 如何协作？
- [x] 上游能否作为固定版本依赖被独立 Demo 获取并构建？
- [x] 同一求解器能否驱动非生物建筑与可读品牌字形，并在多种状态间保持可逆物理因果？
- [ ] 不同桌面和移动 GPU 上的帧率、显存与兼容性表现如何？

## 结论摘要

- **已验证：** 上游使用 `three/webgpu` 和 TSL Compute；默认创建 10 只水母，钟口、口腕和触手共享一套 GPU 弹簧位置缓冲，渲染顶点直接读取该缓冲。
- **已验证：** 按循环结构静态推导，场景约有 32,400 个物理点与 92,920 根弹簧；主体网格、浮游物、光柱和 Bloom 同样为程序化生成。
- **已验证：** 本项目能够通过固定 Git commit 的 npm Git 依赖获取上游源码，并由独立 Vite 子项目构建。
- **推断：** `MedusaVerletBridge` 所代表的“解析锚点 + GPU 次级运动”模式，比水母这一具体视觉更有复用价值。
- **评价：** 它是优秀的 WebGPU 教学和创意技术参考，但缺少公共 API、类型、测试、降级路径和通用求解器边界，不应直接作为生产物理库。
- **R3 已验证：** 形态层与输入层可以拆开；同一低/中/高频状态接口已经驱动流体花、能量织带与数据生命体，说明可迁移价值不局限于水母或软体生物。
- **R6 已验证：** 解析锚点不只可以跟随水母钟体，也可以编码字形记忆；1,264 个可见品牌节点能由同一 GPU 弹簧网络展开为空间轨道并连续召回为可读 AURELIA。

## 能力地图

| 能力 | 画面证据 | 关键实现 | 可迁移价值 |
| --- | --- | --- | --- |
| 解析钟形体 | 周期性收缩、边缘波纹 | `medusaBellFormula.js` | 用少量参数生成可控主动画 |
| GPU 弹簧软体 | 钟口与触手拖尾 | `physics/verletPhysics.js` | 并行处理大批次次级运动 |
| 主体—软体桥接 | 根节点随钟体、末端自然摆动 | `medusaVerletBridge.js` | 混合确定性动画与物理 |
| 缓冲驱动表面 | 离散点形成管状触手和带状口腕 | `medusaTentacles.js`、`medusaOralArms.js` | 避免 GPU→CPU 位置回读 |
| 程序化光色 | 花纹、雾、浮游物、光柱 | `medusaBellPattern.js`、`background.js`、`godrays.js` | 无外部贴图的统一视觉系统 |
| MRT Bloom | 交互时颜色和辉光同步变化 | `app.js` | 材质级选择性后处理 |

## 架构速览

```text
CPU：创建拓扑、弹簧邻接、每只水母的位姿与相位
  ↓ bake
GPU Storage Buffer：position / force / spring endpoints / adjacency
  ↓ 每个固定子步
Bridge Kernel：把固定根节点重新绑定到解析钟形体
  ↓
Spring Kernel：每根弹簧计算伸缩力
  ↓
Vertex Kernel：按邻接表汇总力、阻尼并更新位置
  ↓
Node Material：读取位置缓冲，重建表面并生成颜色、透明度、雾
  ↓
MRT：scene color + bloomIntensity/charge → Bloom → 最终输出
```

详细数据结构、规模推导与物理边界见 [`notes/architecture.md`](notes/architecture.md)。

## 关键实现

### 1. 解析主体

`getBellPosition(phase, zenith, azimuth, bottomFactor)` 用正弦相位控制半径和极角，并在靠近钟口处混入 16 次环向波纹及时变噪声。钟体无需骨骼、模型或 morph target。

### 2. GPU 弹簧

`VerletPhysics.bake()` 将对象图转成适合 GPU 的连续数组：顶点位置、弹簧端点、静止长度、每个顶点的邻接范围，以及带正负方向的弹簧索引。运行时先按弹簧计算力，再按顶点汇总。

### 3. 解析锚点与软体桥

固定点每个子步由钟体解析公式重新定位，非固定点继续接受弹簧力。这使钟体收缩能够稳定传递到钟口、口腕和触手，而不需要 CPU 逐点更新。

### 4. GPU 表面重建

触手渲染顶点只保存两个物理点 ID、截面角和半径；口腕渲染顶点保存四个物理点 ID及侧向偏移。顶点着色器读取物理位置后计算切线、法线和表面位置。

### 5. 光色与后处理

TSL 节点生成噪声花纹、距离雾、焦散式环境光和浮游物。材质通过 MRT 输出发光强度与 `charge`，Bloom 只作用于需要辉光的区域。

## 实验与验证

| 验证问题 | 方法 | 结果 | 证据 | 限制 |
| --- | --- | --- | --- | --- |
| 上游是否可获取 | 克隆并检出完整 commit | 成功固定到 `724aba2` | 上游 commit 链接 | 未依赖 tag/release |
| 上游基线能否构建 | `npm ci && npm run build` | 成功 | 本地命令记录 | 不代表 GPU 运行正确 |
| 能否作为子项目依赖获取 | 固定 commit 的 npm Git 依赖 + 构建前 TSL 转换 | 成功，22 个上游源码文件可重复准备并构建 | Demo `package.json`、`scripts/prepare-upstream.mjs` | 上游没有公共 exports，升级可能破坏适配 |
| 能力说明是否脱离画布可读 | 桌面与 390×844 视口可视检查 | 通过，说明和控制为独立 DOM，移动端无横向溢出 | 本地验证截图（不入库） | 尚未覆盖屏幕阅读器专项审计 |
| WebGPU 真实运行 | Windows Chrome 硬件 WebGPU 启动，操作弹簧与脉冲 | 通过，单画布运行、控件状态正确、控制台无错误 | 本地 Puppeteer 运行记录（不入库） | 单机样本，观察到约 26 FPS，不代表跨 GPU 基准 |

## 优点与局限

### 优点

- 全程序化，研究不被外部模型与贴图质量干扰；
- CPU 与 GPU 职责清晰，物理结果不需要逐帧读回；
- 主动画与次级运动连接自然，视觉结果具有较高完成度；
- MIT 许可证允许在保留声明的前提下修改和再发布。

### 局限

- 上游 `package.json` 标记为 `private`，没有公共 API 或语义化发布；
- 入口主动拒绝非 WebGPU 后端，而 Compute 路径也没有 CPU/WebGL 替代；
- 名称虽然使用 Verlet，但积分没有保存上一帧位置，更接近固定步长的阻尼速度—弹簧积分；
- 固定 360 Hz 子步会在典型 60 FPS 下产生多次串行 Compute 调度；
- 大量对象关闭视锥裁剪，透明对象依赖手工排序；
- 没有碰撞、自碰撞、质量、XPBD 柔度或正式性能基准。

## 可复用结论

- 将可控主体写成解析函数，再把软体根节点绑定到函数表面，可以同时获得设计可控性和自然次级运动。
- GPU 模拟数据直接供顶点着色器消费，比每帧读回 CPU 再更新几何更适合高密度程序化生物。
- 把发光强度作为 MRT 数据显式输出，可以建立材质级而非整屏级的后处理控制。
- 创意 Demo 的物理参数通常与固定步长深度耦合；抽取成库前，应先明确积分器、单位和稳定性契约。

## R2：扩展实验室

在保留默认能力演示的基础上，新增独立的 [`lab.html`](../../apps/001-holtsetio-aurelia/lab.html)。它把“值得复用”进一步变成可观察的工程问题：

- 轻量、均衡、完整三档分别创建 1、3、10 个真实实例，而不是仅切换可见性；
- GPU 物理工作集随档位从 3,240 点 / 9,292 弹簧扩展到 32,400 点 / 92,920 弹簧；
- 固定子步频率可选择 120、240、360 Hz，并同时约束 DPR 和浮游物密度；
- 生命、结构、能量视图分别强调主次动画桥接、弹簧约束和 `charge`—Bloom 状态联动；
- 阻尼与 Bloom 可以实时调节，档位切换则通过整页重建保证 GPU 资源释放边界清晰。

这次扩展也暴露并修正了一个重要的产品化问题：上游出生位置用 `medusaId / 10` 写死。实例数下降后，水母会全部生成在镜头下方。构建适配只在实验室配置存在时按实际实例数重新居中，基线继续执行原公式。详细契约和验证矩阵见 [`notes/extension-lab.md`](notes/extension-lab.md)。

## R3：声音驱动形态实验室

新增独立的 [`morph.html`](../../apps/001-holtsetio-aurelia/morph.html)，回答“只能沿水母这一种形态扩展吗”：不能，也不必。这个入口保留 Aurelia 的实时程序化思想和深海视觉语言，但不复用上游水母几何或物理实现；它刻意建立三种不同的结构语言：

- 流体花使用放射花瓣、核心与实例化花粉；
- 能量织带逐帧更新带状 `BufferGeometry`，形成连续传播表面；
- 数据生命体把实例节点、动态图边和轨道组合成网络对象。

浏览器内原创 Web Audio 生成低频脉冲、五声音阶和声与高频颗粒，`AnalyserNode` 将真实频谱压缩为 `low / mid / high` 三个连续控制量。各形态以不同映射消费这三个值，切换形态时音频时间线保持连续。由此形成一个更通用的结构：

```text
音乐 / 实时数据 / 用户动作 / 业务事件
                 ↓ 标准化连续状态
     low · mid · high（可替换为任意命名参数）
                 ↓ 映射层
  花瓣 / 织带 / 节点网络 / 未来的新形态
```

对我们的直接价值不是“多三个视觉效果”，而是得到一个可继续产品化的实时表现内核：输入源、状态映射和形态渲染彼此解耦。它可以进入品牌开场、音乐可视化、展览装置、数据叙事、实时舞台背景与产品状态表达；下一步若接入已有音乐，仅需增加媒体元素或文件源适配器，若接入业务数据则可直接替换 analyser 输出。详细设计边界和验证结果见 [`notes/morph-lab.md`](notes/morph-lab.md)。

## R4：Sonic Genesis 视觉重构

R3 证明了架构可替换，但画面仍是常规材质与基础几何组成的技术草图，缺少原水母的 GPU 次级运动、程序化表面、空间氛围和后处理协同。R4 没有用更强的亮度掩盖这个差距，而是重建了视觉层级：

- 三种形态都拆成主体、内部结构、能量轮廓、粒子/轨迹、空间光场五层；
- 星云花使用 30 片实例化曲面膜翼、合并光丝、晶核、光环与两类轨道粒子；
- 时空织体使用 14 条实时重建的带状膜、双重环面结、流动节点与空间门；
- 量子生命体使用嵌套晶体、210 个神经节点、动态图边、七重轨道与外层卫星；
- WebGPU PostProcessing 提供受控 Bloom，高频峰值与低频起音通过对象池触发冲击波和碎片喷发；
- 形态切换采用可中断的离场—重组—入场状态机，快速连续切换不会遗留多个场景；
- 桌面限制渲染分辨率以平衡多层透明与 Bloom，窄屏减少粒子/节点并改变镜头距离。

这次重构的意义是把“可复用内核”推进到“可用作主视觉的表现系统”：连续参数负责形变，离散峰值负责视觉事件，转场状态机负责叙事连续性，质量预算负责不同设备。它比单独复制水母造型更接近可持续扩展的创意技术资产。

## R5：Creative Directions 与 Resonant Matter

新增 [`directions.html`](../../apps/001-holtsetio-aurelia/directions.html) 与 [`resonance.html`](../../apps/001-holtsetio-aurelia/resonance.html)。前者把横向扩展整理成十个有顺序、有状态的作品方向；后者先完成 Direction 01「声场雕塑」，避免一次生成大量低完成度换皮形态。

Resonant Matter 将数据链重新接回上游 `VerletPhysics`：桌面由 24 片交叉张拉膜组成，实际作品拓扑为 3,120 个 GPU 节点和约 13,200 个约束，以 180 steps/s 求解。音频不会直接缩放场景，而是改变上下锚环的半径、跨度、相位和局部行波；锚点变化经过弹簧计算与位置积分后，膜面 `positionNode` 和弹簧线框再从同一 storage buffer 读取结果。TSL MRT 只对材料声明的结构边缘做选择性 Bloom。

实现中真实 GPU 回读发现自定义独立拓扑的 position storage index 0 会出现非有限值，并沿约束传播。Direction 01 隔离了一个不参与表面和约束的 guard 槽，同时降低高连接度膜网刚度并增加耗散；最终回读确认全部 3,120 个作品节点保持有限，半径约为 2.75–5.85。这个问题也说明直接复用求解器并不等于直接复用水母参数：新的拓扑仍需要稳定性校准。

完成后的视觉不再以中心球或粒子烟花为主角，而是上下锚环、交叉膜翼、暗色虹彩表面和弱线框构成的建筑性轮廓。硬件 Chrome 最终短时观察约 79–87 FPS，移动档位自动降为约 1,600 个节点、6,700 根约束和 120 steps/s；这只代表当前机器。Direction 02–05 依次为活体品牌、可演奏空间、引力织体和数据生命网络，另外记录记忆合金、声学黑洞、拓扑气候、晶体语法与群体字形作为候选。详细契约与证据见 [`notes/creative-directions.md`](notes/creative-directions.md)。

## R6：Living Identity / 活体品牌

新增 [`identity.html`](../../apps/001-holtsetio-aurelia/identity.html)，把 Direction 02 从路线图推进为第二件独立作品。它先在离屏 Canvas 中按设备档位采样 AURELIA 字形，再为每个样本生成两类物理点：隐藏的固定形状记忆锚与可见的自由节点。零静止长度 tether 负责召回，同字母的水平、纵向、对角与稀疏远邻弹簧负责局部连续性。

桌面拓扑包含 1,264 个可见节点、2,529 个总顶点（含 1,264 个锚点和 1 个隔离 guard）以及 6,382 根约束，以 160 steps/s 求解。解构不是从字形模型切到轨道模型：GPU bridge 连续把同一批锚点从平面字形插值到七个环向局部拓扑面，Aurelia 弹簧再拖动自由节点，渲染节点和结构线始终读取同一个 position storage。凝聚力滑杆还会在 GPU 上改变每根 tether 的 stiffness。

低频控制环形拓扑的空间深度，中频改变全局轨道相位，高频沿节点相位产生电荷并触发池化的信号切片；召回用有上限的椭圆环表达形状记忆。静音保留 analyser 和视觉，暂停后频谱衰减且瞬态对象归还对象池。无 WebGPU 时，页面保留真实 DOM 字标、概念/状态说明和可运行声音，而不伪造计算动画。

真实 GPU 回读确认全部 1,264 个显示节点为有限值，完全解构后的空间包围盒约为 `x -4.14..4.16 / y -1.28..1.25 / z -3.05..3.25`。桌面多个状态的本机短时观察约 55–67 FPS；移动档位自动变为 654 个显示节点、约 3,100 根约束、110 steps/s，并通过窄屏字宽和镜头策略保持完整字标。方向 01 保持不变，方向总览现将 01/02 标为 `LIVE`、03 标为 `NEXT`。设计、VFX/音频预算和完整验证矩阵见 [`notes/living-identity.md`](notes/living-identity.md)。

## 展示素材

本条目不提交上游截图或生成封面；本地浏览器验收截图分别保存在被 Git 忽略的 `validation-artifacts/001-aurelia/`、`validation-artifacts/001-aurelia-morph/`、`validation-artifacts/001-aurelia-directions/`、`validation-artifacts/001-aurelia-identity/`、`validation-artifacts/001-aurelia-atlas/` 与 `validation-artifacts/001-aurelia-rain/`。素材记录规则见 [`assets/README.md`](assets/README.md)。

## R8：Field Atlas / 创意研究图谱

新增 [`atlas.html`](../../apps/001-holtsetio-aurelia/atlas.html)，把此前分散的方向构想整理为一条可持续复用的方法链：`现实观察 → 物理抽象 → 技术模块 → 交互扩展 → 产品形态`。图谱保留原规划的十个方向与真实交付状态，并加入雨伞、晾衣绳、地铁人流、毛笔墨水、树影窗帘、磁铁铁屑和群体同步七类现实观察。

每个观察都明确固定点、自由度、约束、场、冲击、时间和可输入变量，再连接到九层技术研究与十种指针/交互模块。页面支持状态筛选、案例详情、方向/技术/模块联动高亮和 hash 深链；禁用 JavaScript 时核心正文仍完整保留。详细内容、研发优先级和浏览器覆盖记录见 [`notes/research-atlas.md`](notes/research-atlas.md)。

## R9：图谱查漏补缺

在 R8 初版上新增蛛网露珠、潮汐沙面、玻璃凝露、鸟群鱼群与根系菌丝，使现实样本从 7 个扩为 12 个，并补足脆性碰撞、侵蚀沉积、相变、群体运动和生长修剪机制。技术研究从 9 层扩为 13 层，将接触/碰撞、历史/重放、空间声化与可观测性从大类描述中独立出来。

十个原规划方向现在各自拥有下一最小实验、缺失内核、可量测成功标准与产品桥接；十个交互模块增加四种组合配方，并用因果可读、数值稳定、预算明确、数据诚实和降级保义五项质量门约束作品化。Web 页面同时增加现实案例的机制筛选与第七章实验矩阵，详细 R9 覆盖证据仍记录在 [`notes/research-atlas.md`](notes/research-atlas.md)。

## R10：Rain Membrane / 雨落伞面

新增 [`rain.html`](../../apps/001-holtsetio-aurelia/rain.html)，把图谱中的第一个现实观察推进为可运行作品。伞面使用 854 个 GPU 节点与约 3,300 根约束：外缘、中心和 12 根伞骨的隐藏固定锚维持结构，其他节点经 Aurelia `VerletPhysics` 接收有起点、有传播时间和衰减的冲击力。膜面、约束线、雨滴、飞溅和积水高光都读取或映射同一组冲击与湿润状态。

该样例新增三个可迁移内核：八槽冲击历史、可变湿载荷/张力桥和 GPU 能量边界。快速重击曾使自由节点超过材料允许范围；最终实现用速度预算、相对基形位移预算和 GPU reset kernel 保证可恢复，同时保留可见的局部响应。Web Audio 雨琴将张力与积水映射到基频、滤波和撞击音阶，不依赖外部音乐文件。

## R11：三维观察交互修复

Rain Membrane 现在把“观察”和“施力”分成同一指针的两种明确意图：移动超过 7px 就进入受限轨道相机，抬起不会制造雨滴；轻点则在抬起时只生成一次局部冲击。相机围绕伞面中心连续环绕并限制俯仰，避免钻入膜面或翻转；键盘方向键、Enter/Space 与 0 分别提供旋转、冲击和视角复位。

该修复的意义不是额外加一个 3D 控件，而是让空间结构可被主动阅读：用户可以从不同角度判断伞骨锚点、膜面下坠、波前与积水之间的关系。完整设计契约和浏览器证据见 [`notes/rain-umbrella.md`](notes/rain-umbrella.md)。

## R12：拖动语义与程序化伞架

根据实际拖动反馈，水平轨道从 camera-orbit 语义改为 object-grab 语义：向右拖和 ArrowRight 都让 yaw 正向增加，拖动仍不会产生雨滴。原先独立的直杆与部分圆环会在侧视投影中产生断口；现在中棒到 J 形握把是一条连续空间曲线，并加入握把包覆、封帽、中棒活动套环与 12 根下撑骨。

当前雨伞确实由代码生成，而不是 AI 图片或下载模型：动态膜面必须继续与 Aurelia 节点、弹簧、冲击和湿载荷共享拓扑；静态伞架则采用约 13,080 triangles 的程序化 Three.js 几何。若追求更高写实度，推荐后续使用混合资产路线——先用生成模型形成多视图概念，再把经过拓扑清理和尺度规范的 GLB 只用于伞架硬件，保留现有代码驱动膜面。这样既提升外观，也不丢失实时物理。

## R13：二维抓取语义闭合

R12 只修正了水平符号；纵向仍是传统轨道相机语义，因此向上拖会抬高观察者，让伞面特征在屏幕里向下移动。R13 同步反转纵向输入和 ArrowUp / ArrowDown，使 X/Y 都表现为“抓住作品向同方向运动”，并降低俯仰灵敏度、提高拖动跟随速度。握把材质也由高发光酸绿收敛为深瓶绿湿漆，让形体可辨但不抢主视觉。

它证明的不是“能画一把伞”，而是 `离散事件 → 约束传播 → 时间积累 → 系统再调谐` 可以成为可复用体验语法。相同机制可以迁移到建筑膜面、天气品牌、触觉界面、实时降雨数据雕塑和材料状态声化。实现边界、设计契约与浏览器矩阵见 [`notes/rain-umbrella.md`](notes/rain-umbrella.md)。

## R14：因果叙事稳定化

因果链原先直接读取自动雨产生的最新冲击；中雨约每 0.28s 重置一次事件，UI 又同时使用活动行横移和 600ms 无限亮度脉冲，因此在连续画布上形成高频跳变。R14 将物理事件与展示节奏解耦：稳态雨保持积累阶段，只有用户主动落雨才按冲击、传播、积累的顺序展开，排水进入再调谐。

界面取消循环闪烁和行位移，阶段只在真正改变时写入 DOM，并以 `aria-current="step"` 标记当前步骤。修复没有降低雨滴频率或物理活性，而是把高频模拟压缩为人能读懂的状态叙事。

## R15：雨幕材质与冲击表现

画面本体现在用五层连续反馈表达同一物理链：暖色接触核表示动量进入膜面，青色主波和暖色回波表示传播，定向水冠表示局部反弹，径向细流与高光珠表示积水沿坡度迁移，湿润材质则把 wetness 映射为更低粗糙度和更集中反射。伞骨之间的程序化 panel sag 让冠面具有清楚的 12 瓣张力，而不是平滑圆盘。

这些增强没有改写 Aurelia 的 GPU 节点和约束拓扑。波前、飞溅和径流均使用固定对象池；移动端减少实例与路径，reduced-motion 减少水冠并停止径流珠迁移，reset 会清理所有短效。完整规格与浏览器证据见 [`notes/rain-umbrella.md`](notes/rain-umbrella.md)。

## R16：Weatherproof 业务官网

[`weatherproof.html`](../../apps/001-holtsetio-aurelia/weatherproof.html) 不再把物理效果当作独立展品，而是把它放入“进入品牌 → 选择天气 → 获得推荐 → 比较型号 → 预约体验”的业务路径。三种场景同时驱动复用的 `RainMembraneScene`、推荐 SKU、推荐理由和深度实验链接；预约流产生可观测的前端事件，为后续分析平台与 CRM 接入保留边界。

现阶段使用清楚标注的虚拟品牌、价格与评分，不冒充真实商品或检测结论。真实业务数据、支付、库存、用户系统与外部服务不在本轮范围内。设计契约和覆盖清单见 [`notes/weatherproof-business.md`](notes/weatherproof-business.md)。

## R17：可替换商品数字样机

Weatherproof 的产品选择不再只更新文案：`RainMembraneScene` 新增产品 profile 接口，三种样机分别改变膜面材质、骨架颜色、8 / 12 / 14 骨可视结构，以及基础张力、遇水软化、蓄水和排水倾向。默认研究页仍使用原有共享求解器；官网明确说明可视结构和参数是演示样机，真实上线必须由 CAD/GLB、PBR 材质扫描和实验室曲线校准。

技术区同时给出四层真实接入路径：商品几何、真实材料、实测响应、业务数据。它说明大模型适合做概念方向和材质预览，但真实商品体验的可信度最终来自产品资产与可验证数据，而不是凭空生成一张更像真的图片。

## R18：合成数据驱动的产品证据

[`weatherproof-demo.json`](../../apps/001-holtsetio-aurelia/src/data/weatherproof-demo.json) 构造了一套版本化演示数据：三组天气输入、三个虚拟 PIM/SKU、价格与区域库存快照、实验批次，以及张力、滞水、风速形变三类六点曲线。Weatherproof 新增合成数据工作区，场景与产品选择现在会同时更新 3D、推荐、商品摘要、数据批次、对照曲线和 HTML 数据表。

它验证的是“真实接口接进来后，状态如何流经整条用户旅程”，而不是证明这些虚拟商品真的通过了测试。完整字段、取舍和真实替换路径见 [`notes/weatherproof-synthetic-data.md`](notes/weatherproof-synthetic-data.md)。

## R19：Windline / 风中的晾衣绳

新增 [`clothesline.html`](../../apps/001-holtsetio-aurelia/clothesline.html)，把图谱 `FIELD 02` 推进为第二个可运行现实机制样例。桌面拓扑用 386 个 GPU 节点和约 1,800 根约束连接固定端、链式绳索与三件不同面积/质量织物；移动端使用 233 个节点的降档拓扑。常风、局部阵风、负重和阻尼经桥接层写入 Aurelia force storage，地面软碰撞、速度预算和相对基形位移预算共同限制能量。

本样例把直接操控与镜头分离：在画布上拖向哪里，阵风就按屏幕同方向作用，向上拖会产生真实抬升分量，不再借用轨道相机语义。结构透视、传播阶段、实时指标和 GPU readback 让“固定 → 负重 → 局部受风 → 链式传播 → 阻尼恢复”可被观察和验证。设计契约与覆盖证据见 [`notes/wind-clothesline.md`](notes/wind-clothesline.md)。

### R21：从程序化占位物到可追溯专业资产

用户指出“素材应该用最好的素材，而不是代码生成”。因此 Windline 保留 Aurelia 物理拓扑，但废弃“用程序轮廓承担最终美术”的假设：中央服装换为 Smithsonian Open Access 的 CC0 摄影测量 `Feedsack Dress` GLB，两侧材料换为 Poly Haven CC0 的 Denim Fabric 02 与 Terlenka 实拍 PBR。服装的 56K+ 原始顶点不是静态摆件，而是以四节点双线性权重绑定到 13×20 的 Aurelia 弹簧代理面；代理面承担低频形变，扫描网格保留高频褶皱、厚度和纹理。桌面拓扑提升为 591 节点 / 约 3,000 约束，强阵风后的 GPU readback 保持有限，观察帧时约 4.2ms。资产来源、许可和运行体积记录在 [`ASSET_SOURCES.md`](../../apps/001-holtsetio-aurelia/public/assets/clothesline/ASSET_SOURCES.md)。

## R25：Metro Pulse / 地铁人流与拥堵传播

新增 [`metro.html`](../../apps/001-holtsetio-aurelia/metro.html)，把图谱 `FIELD 03` 推进为第三个可运行现实机制样例。15 个合成站点通过隐藏容量锚、自由物理节点和 32 根 tether / 跨线弹簧接入 Aurelia；四条线路与 150 个桌面客流粒子持续读取同一网络状态。客流模型负责需求、容量下降、邻接扩散和恢复，GPU bridge 把压力写入 position / force storage，使拥堵同时表现为节点升高、线路形变、热色、粒子减速与传播环。

用户可以轻点任一 3D 站点注入客流，拖动只负责受限环绕而不会误触业务事件；晚高峰、活动散场和中央封站三种情景分别演示持续需求、短时脉冲和容量骤降。所有值都标记为合成数据，不用于交通预测。设计契约、状态向量与浏览器矩阵见 [`notes/metro-crowd.md`](notes/metro-crowd.md)。

## R26：Living Ink / 活体书法

新增 [`ink.html`](../../apps/001-holtsetio-aurelia/ink.html)，把图谱 `FIELD 04` 推进为第四个可运行现实机制样例。它不沿用前序样例的暗色监控台：宣纸留白成为空间舞台，墨迹是第一焦点，朱砂只做落款，参数与技术证明退到纸缘。页面因此在构图、颜色、材质、字体、控件和运动上形成新的视觉家族，而不是一次换色。

11 组四段式鬃毛链构成 44 个 GPU 节点和 53 根 Aurelia 弹簧约束。压力改变接触宽度，速度参与飞白/飞墨阈值，含水量和纸张吸收共同决定最多 320 个湿样本的扩散寿命；停笔后，墨迹永久沉积而活跃状态逐步收敛。浓墨、淡墨、朱砂、自动示范、清纸和可选结构透视共享同一因果链。桌面、390px、reduced-motion 和 2D fallback 的真实拖动均已验证，GPU readback 为 `FINITE · 44 nodes · z 0.299..0.725 · 4.2ms`。完整设计契约和浏览器证据见 [`notes/living-ink.md`](notes/living-ink.md)。

## R27：Veil of Light / 树影与窗帘

新增 [`curtain.html`](../../apps/001-holtsetio-aurelia/curtain.html)，把图谱 `FIELD 05` 推进为第五个可运行现实机制样例。视觉系统不再沿用暗色监控台或纸本书法，而是让一间暖色日光房持续占据首屏：亚麻帘、中央窗景、窗台、日光池与灰绿树影先建立空间尺度，控制与技术证明退到边缘。

双幅窗帘在桌面由 1,344 个可见节点和 7,548 条 Aurelia 弹簧约束组成；移动端为 704 个节点和 3,848 条约束。风强、开合锚点、人的靠近场与拖动尾流进入同一 GPU force / position storage，布面形变继续改变动态树影密度、光照色温与 Web Audio 的风、叶、室内共鸣比例。晨雾、午后、薄暮三种预设、结构透视、声场解锁、复位与 GPU 诊断均已在真实浏览器验证；一次 R29 桌面读回为 `FINITE · 1,344 nodes · max Δ 0.078 · 13.6ms`。390×844、`?motion=reduce` 和 `?fallback=1` 均无页面溢出并保留可操作语义。完整契约和验证台账见 [`notes/tree-curtain.md`](notes/tree-curtain.md)。

## R28：从程序化房间到混合资产空间

实景评审确认首版“物理是真的、环境却像技术原型”：圆柱树枝、圆片叶群、盒子家具和渐变窗景把视觉上限锁死。因此 R28 删除全部粗糙环境 primitive，改为摄影级建筑底片负责空间、真实植被与材料细节，独立自然树影 gobo 负责帘面遮挡；代码只进行实时裁切、漂移和密度混合。前景仍是 Aurelia GPU 帘布，Terlenka normal / roughness 仍来自 Poly Haven CC0 扫描，因而没有用一张视频或整页图片冒充交互。资产清单见 [`../../apps/001-holtsetio-aurelia/public/assets/curtain/ASSET_PROVENANCE.md`](../../apps/001-holtsetio-aurelia/public/assets/curtain/ASSET_PROVENANCE.md)。

新版在 1280×720 与 390×844 均完成浏览器复核：桌面拖动后局部场为 `0.319`，读回为 `FINITE · 864 nodes · max Δ 1.683 · 7.7ms`；移动端拖动局部场为 `0.503`，页面无溢出；`?fallback=1` 确认继续使用同一摄影底片，控制台无 warning/error。

## R29：动态树冠与正确垂帘

R28 的摄影底片解决了单帧质感，却留下两个结构性问题：窗外的整棵树仍被读成静态背景，窗帘又只锚定顶部，打开后内缘向外扩张成梯形。R29 将两张透明摄影枝叶拆成前后景层，分别使用独立枢轴、相位、频率、振幅与视差；持续风、预设和 pointer drag 共同驱动枝叶层、布面投影、Aurelia 帘布与空间声场。

布料拓扑升级为桌面每幅 `24×28`、移动每幅 `16×22`，按当前开度建立基形，并增加可见挂环、12 道全高纵向褶皱、柔软下摆及整幅横向跟随力。午后默认树冠摆角约 `0.09rad`；390px 真实拖动后树冠达到 `0.217rad`、局部布面场达到 `0.826`。桌面诊断为 `FINITE · 1,344 nodes · max Δ 0.078 · 13.6ms`；移动端为 704 节点 / 3,848 约束，reduced-motion 与动态 2D fallback 继续通过。

## R30：Flagship Arena / 第二旗舰竞技场

新增 [`arena.html`](../../apps/001-holtsetio-aurelia/arena.html)，停止用单机制现实样例承担“替代水母”的任务。水母继续作为 `10.0` 的正式参考入口；光翼生命体、星种绽放与磁流体祭坛在同一 WebGPU 镜头、Bloom、原创声场、pointer 输入和六项统一评分下实时切换。评分明确是研究判断而非营销数据，三个候选均标记为 live prototype，尚未宣称超过或替代水母。

光翼生命体由一张连续动态翼膜、可见骨架、尾丝、发光胸腔和远景群游组成；星种把多层花瓣的收束/展开与数百枚播散光屑组成短叙事；磁流体使用实例化磁刺实时对齐指针控制的场向。候选程序化拓扑属于 L2–L3 原型资产，胜出后才值得升级为 Aurelia GPU compute 物理和 L4 近景材质。完整契约与验收记录见 [`notes/flagship-arena.md`](notes/flagship-arena.md)。

## R31：Lumen Cuttlefish / 墨光乌贼群

R30 的同视口对照证明三个候选都没有达到原水母的系统完整度，早期 `8.6` 自评分不再作为成熟度证据。研究方法因此改为“先选择主题，再集中完成一条因果链”：从静帧轮廓、结构受力、材质角色、单体/群体、输入语义、短叙事与程序化资产上限七个方面筛选，最终选择墨光乌贼群。

新增 [`cuttlefish.html`](../../apps/001-holtsetio-aurelia/cuttlefish.html)。英雄乌贼拥有程序化虹彩皮肤、独立眼睛、连续鳍膜和接入 Aurelia `VerletPhysics` 存储缓冲的真实触腕弹簧；指针先改变凝视和局部色素，`SIGNAL` 触发全身传播，`INK` 触发墨云、上浮与远景群体散开，结构层展示真实约束。完整选题矩阵与 R31 契约见 [`notes/flagship-arena.md`](notes/flagship-arena.md) 和 [`notes/lumen-cuttlefish.md`](notes/lumen-cuttlefish.md)。

### R32：从乌贼主题到乌贼运动

用户复核指出 R31 仍像竖直的程序化水母替身，也没有明确的点击因果。R32 将主体改为斜向游姿，使用内置 ImageGen 生成并接入真实色素胞皮肤细节，缩窄外套膜并收紧连续鳍缘；自主状态加入轮廓行波、外套膜喷射收缩和空间巡游。点击主舞台现在会执行几何命中检测，并同时触发冲击环、局部色素传播、身体反向避让和 GPU 触腕外力。详细资产记录、运行证据和写实上限见 [`notes/lumen-cuttlefish.md`](notes/lumen-cuttlefish.md)。

### R33：从动态模型到完整因果动物

R33 不再依赖增加光效改善观感，而是修正生命体各层之间的关系：加入腕冠遮蔽头腕连接缝，把双眼改为侧向嵌入，使用不依赖背光方向的节点材质保持腕足可读，并加入虹吸结构、喷流体积和压力环。巡游、观察、惊跳与防御成为可观察状态；外套膜收缩、喷流、反向位移、鳍膜和腕足延迟位于同一动力链。镜头开放受限且方向一致的 3D 拖动，性能侧复用射线/指标对象并把鳍膜法线降为隔帧更新。桌面、窄屏、键盘、低动态和降级证据详见 [`notes/lumen-cuttlefish.md`](notes/lumen-cuttlefish.md)。

### R34：让默认运动无需解释也能被看见

真实页面复核发现 R33 的动画循环虽然运行，默认巡游在 1.5 秒窗口内却只有约 0.02 的位移，且 `sin^10` 喷射脉冲过窄，用户很容易把它判断为静态。R34 将运动拆成慢速椭圆巡游、中速航向与滚转、快速鳍缘行波、腕足惯性和约四秒一次的宽喷射事件。708×898 实测 1.5 秒由 `0.58,0.85,1.13` 移至 `0.23,1.01,1.11`，5 秒内喷射峰值达到 `thrust=0.910`；桌面、点击、拖动、低动态与 CSS fallback 均已复测。完整前后证据见 [`notes/lumen-cuttlefish.md`](notes/lumen-cuttlefish.md)。

### R35：让运动方向、身体压力与尾流成为同一件事

R34 让运动被看见，却仍可能在强喷射窗口里朝着身体推进轴的反方向漂移，外套膜也会纵向缩短、横向变胖。R35 用闭合轨迹的解析切线驱动身体航向，把外套膜改为径向预压、轴向保持，再让虹吸推力、鳍膜收拢、压力环、微泡与 GPU 触腕水阻依次响应。7.6 秒真实周期中高推力对齐度保持 `0.976–0.996`；`thrust=0.950` 时径向尺度 `0.955`、轴向尺度 `1.010`。点击、拖动、SIGNAL、结构层、reset、reduced-motion 和 CSS fallback 均已复测，完整证据见 [`notes/lumen-cuttlefish.md`](notes/lumen-cuttlefish.md)。

### R36：让近景结构也经得起观察

R35 的运动因果成立后，近景仍暴露光滑管状腕足、捕食腕末端不明确、皮肤只有颜色纹理以及鳍根缺少连接线。R36 用单一合并几何生成成对浅杯吸盘，并让每个吸盘直接读取对应腕足的 GPU 物理端点；两条捕食腕在末端形成宽触腕棒和更密吸盘。外套膜增加低振幅稳定微起伏，鳍根缝线与鳍面共享实时顶点。708×898 实测 92 个吸盘、约 4304 个统计三角面，1280×720 为 180 个吸盘、约 10176 个统计三角面；完整证据与资产边界见 [`notes/lumen-cuttlefish.md`](notes/lumen-cuttlefish.md)。

### R37：纠正运动语法、解剖数量与三维阅读

用户复核发现 R36 虽然使用真实 WebGPU 三维网格，却仍因周期性自动喷射、持续平面转向和过窄镜头范围而被读成一张会转的软体图；窄屏还错误删掉两条普通腕。R37 将默认状态改为周缘鳍巡航，只有点击或墨云才进入 `compress → jet → recover`；预压样本为 `squeeze=0.175 / thrust=0`，主喷射样本为 `thrust=0.589 / alignment=0.975`，峰值推进轴对齐度达到 `1.000`。所有质量档固定为 8 腕 + 2 捕食触腕，SIGNAL 单独维持展开能量并显露末端触腕棒和吸盘。

初始三分之四构图外，页面新增 HERO / PROFILE / CLOSE 三档视角并扩大自由轨道范围；507×898 的真实拖动让相机由 `(0, 0.4, 19.2)` 变为 `(6.4, 5.84, 17.38)`。最终桌面为 200 个 GPU 物理点、418 条约束、156 个吸盘和 10 条附肢，窄屏为 168 / 342 / 100 / 10；两个尺寸均无页面溢出。该版本仍诚实标记为 L2–L3 混合程序化资产，不冒充扫描级自然历史模型。完整纠错记录与浏览器矩阵见 [`notes/lumen-cuttlefish.md`](notes/lumen-cuttlefish.md)。

### R38：实体皮肤与连续头胴

不透明度提升后首先暴露出 R37 隐藏的球形头接缝、白色吸盘团块和错误的轴向 PROFILE 镜头。R38 删除独立头球与大腕冠，以同一外套膜剖面连续形成头胴；皮肤透射降至 `0.08`，斑驳纹理退为受控细节层，眼睛嵌入侧面，虹吸移至腹侧，腕体改为实体遮挡而吸盘只在结构层增强。PROFILE 改成三分之四相机，HERO / PROFILE / CLOSE 现在共同证明宽扁外套膜、鳍面、眼与腕束的前后关系。

1280×720 与 390×844 真实浏览器均无溢出或日志错误；窄屏冷启动继续保留 10 条附肢。点击主喷射样本为 `thrust=0.730 / alignment=0.998 / wake=0.966`，随后恢复鳍巡航；SIGNAL 仍独立展开捕食触腕。完整修订与资产上限见 [`notes/lumen-cuttlefish.md`](notes/lumen-cuttlefish.md)。

## R39–R40：Project Synthesis / 项目研究总账

R39 首先新增 [`summary.html`](../../apps/001-holtsetio-aurelia/summary.html)，把此前分散在 15 个案例和多份研究记录中的结论组织为统一网页。首版解决“项目是什么、案例分别承担什么职责、为什么水母仍是基准、当前价值与决策是什么”，但仍偏管理层总览。

R40 根据页面复核继续向下补齐事实层：十五个案例均拥有结构规模、输入、同源输出、验证结果和真实边界；六阶段运行链解释 CPU 拓扑如何进入 Storage Buffer、固定步长 Compute、表面绑定和多通道反馈；能力矩阵横向核对 10 个主要案例的 GPU 物理、场、表面、指针、声音、业务数据、真实资产和降级路径；十个方向、十二个现实映射、R0–R40 七阶段时间线和研究来源索引也被完整展开。

页面默认只展开水母基准，允许逐条或批量展开证据。1280×720、768×1024 与 390×844 实测均为 0px 页面级横向溢出；窄屏矩阵保留局部滚动。键盘、低动态、无 JavaScript 原生内容和 production build 均通过。该页面现在承担“项目研究总账”而非新的视觉案例，线上入口为 [`summary.html`](https://yydshly.github.io/0902_codex_project/demos/001-holtsetio-aurelia/summary.html)。

## Web Demo

- 源码：[`apps/001-holtsetio-aurelia/`](../../apps/001-holtsetio-aurelia/)
- 在线地址：`https://yydshly.github.io/0902_codex_project/demos/001-holtsetio-aurelia/`
- 在线研究总账：`https://yydshly.github.io/0902_codex_project/demos/001-holtsetio-aurelia/summary.html`
- 验证边界：运行真实上游实现，并解释四类核心能力；不证明科学准确性、全浏览器兼容性或生产性能。

## 来源与相关链接

- [上游仓库](https://github.com/holtsetio/aurelia/)
- [固定研究基线](https://github.com/holtsetio/aurelia/tree/724aba263459d1b11a49b72bfc7ce8baac840ca8)
- [Three.js WebGPURenderer](https://threejs.org/manual/en/webgpurenderer)
- [Three.js TSL 规范](https://threejs.org/docs/TSL.html)
- [MDN WebGPU API](https://developer.mozilla.org/en-US/docs/Web/API/WebGPU_API)

## 变更记录

- `2026-09-03`：创建研究条目、固定上游版本并增加能力演示子项目。
- `2026-09-03`：完成构建前 TSL 适配、硬件 WebGPU 交互验证和移动视口检查，状态更新为 `validated`。
- `2026-09-03`：保留基线并新增质量档位、运行指标、观察模式和参数控制扩展实验室。
- `2026-09-03`：新增三种非水母程序化形态、原创 Web Audio 声场与实时频谱映射，完成桌面、移动和降级验证。
- `2026-09-03`：将 Morph Lab 重构为 Sonic Genesis，增加五层形态、WebGPU Bloom、音频瞬态 VFX、可中断转场与移动端质量预算。
- `2026-09-03`：新增 Creative Directions 十方向索引与 Direction 01 Resonant Matter，以真实 Aurelia GPU 弹簧、缓冲驱动膜面和音乐锚点完成非生物声场雕塑。
- `2026-09-03`：完成 Direction 02 Living Identity，以字形采样、隐藏记忆锚、GPU tether stiffness、空间解构与物理召回验证动态品牌方向。
- `2026-09-03`：新增 Field Atlas，将原十方向、七类现实观察、九层技术研究和十种交互模块组织为可查询、可深链的 Web 研究图谱。
- `2026-09-03`：完成 Field Atlas R9 查漏补缺，扩为十二个现实观察、十三层技术研究，并增加四个组合配方、十方向实验矩阵和五项作品化质量门。
- `2026-09-03`：完成 Field Prototype 01 Rain Membrane，以雨滴冲击、伞骨锚、湿载荷记忆、GPU 能量边界和程序化雨琴验证首个现实机制样例。
- `2026-09-03`：完成 Rain Membrane R11 交互修复，加入拖动轨道相机、轻点命中分流、移动端单指操作和键盘等价路径。
- `2026-09-03`：完成 Rain Membrane R12，纠正抓取式拖动方向，并以连续 J 形握把、下撑骨和连接件重建程序化伞架。
- `2026-09-03`：完成 Rain Membrane R13，闭合纵向抓取与键盘方向语义，并降低俯仰灵敏度和握把视觉噪声。
- `2026-09-03`：完成 Rain Membrane R14，解耦自动雨与因果链展示阶段，移除活动行跳动和无限闪烁并补充当前步骤语义。
- `2026-09-03`：完成 Rain Membrane R15，增加接触核、双层波前、定向水冠、湿载荷径流和动态湿润材质，并维持桌面/移动固定预算。
- `2026-09-03`：新增 Weatherproof R16，把场景选择、物理解释、演示产品推荐、型号比较和预约转化连接成首条官网业务闭环。
- `2026-09-03`：完成 Weatherproof R17；三种演示型号开始驱动实时 3D 材料、8 / 12 / 14 骨可视框架与物理响应，并补齐真实商品资产/材料/实测/PIM 接入说明。
- `2026-09-04`：完成 Weatherproof R18 合成数据演示；新增版本化天气、PIM、库存与实验曲线数据源，以及产品联动的对照图和可读数据表。
- `2026-09-04`：完成 Field Prototype 02 Windline；以固定端、三种织物负重、方向阵风、链式传播、阻尼与 GPU 稳定边界验证“不可见风场如何通过结构显形”。
- `2026-09-04`：完成 Field Prototype 03 Metro Pulse；以站点容量锚、线路弹簧、合成客流、拥塞回压与恢复验证“局部压力如何改变离散网络”。
- `2026-09-04`：完成 Field Prototype 04 Living Ink；改用明亮纸本视觉系统，以 GPU 笔锋约束、压力/速度映射、湿墨扩散与时间记忆验证“输入如何沉积为可持续视觉资产”。
- `2026-09-04`：完成 Field Prototype 05 Veil of Light；以双幅 GPU 帘布、局部风场、动态树影遮挡与空间声场验证“环境变量如何被材料显形并形成可交互空间界面”。
- `2026-09-04`：完成 Veil of Light R28 素材重构；移除程序化房间、树枝、叶片与家具，以摄影级建筑底片、自然树影 gobo、CC0 扫描织物和实时 GPU 帘布建立可追溯的混合资产栈。
- `2026-09-04`：完成 Veil of Light R29 动态与垂帘修订；加入双层透明摄影树冠、统一风/拖动冲量，升级布料拓扑、挂环、纵褶和下摆，使树、影、帘和声音形成可读的连续因果。
- `2026-09-04`：建立 Flagship Arena；把水母从“待替代对象”改为正式质量基准，并在同一实时舞台比较光翼、星种和磁流体三个第二旗舰候选。
- `2026-09-04`：完成 Lumen Cuttlefish R35；用解析轨迹切线、分阶段外套膜预压/虹吸推力、速度驱动鳍膜、GPU 触腕水阻和渐隐尾流建立方向一致的完整推进链。
- `2026-09-04`：完成 Lumen Cuttlefish R36；增加与 GPU 腕足共同形变的成对吸盘、捕食腕触腕棒、稳定皮肤微起伏和动态鳍根缝线，并建立混合资产来源记录。
- `2026-09-04`：完成 Lumen Cuttlefish R37；用鳍巡航/事件喷射双模式替换周期自转，固定 8 腕 + 2 捕食触腕，并用三档视角、自由轨道和阶段指标证明真实三维因果。
- `2026-09-04`：完成 Lumen Cuttlefish R38；以连续头胴、实体斑驳皮肤、嵌入式侧眼、腹侧虹吸、低亮度腕束和修正后的三分之四镜头替换透明拼装感，并复测点击喷射、窄屏、低动态与降级路径。
- `2026-09-04`：完成 Project Synthesis R39；把 15 个案例、四层能力、水母基准、研究体系、真实缺口、项目价值和阶段决策组织为统一网页总览。
- `2026-09-04`：完成 Project Synthesis R40；新增十五案例事实账、逐帧技术链、能力矩阵、十方向、十二现实映射、R0–R40 演进与来源索引，并通过桌面、平板、手机、键盘、低动态、无脚本和构建验证。
